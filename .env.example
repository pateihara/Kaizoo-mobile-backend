# 🌐 Banco de Dados (Neon - PostgreSQL)
# Copie a connection string no painel do Neon (Connection Details → Connection string)
# Exemplo:
# postgresql://USUARIO:SENHA@HOST/neondb?sslmode=require&channel_binding=require
DATABASE_URL="postgresql://USUARIO:SENHA@HOST/neondb?sslmode=require&channel_binding=require"

# 🔐 Chaves secretas para geração/validação dos tokens JWT
# Troque por strings seguras em produção
JWT_ACCESS_SECRET="sua_chave_super_secreta"
JWT_REFRESH_SECRET="sua_chave_refresh_super_secreta"

# ⏱️ Expiração dos tokens
# Access tokens curtos (ex: 15 minutos), Refresh tokens longos (ex: 30 dias)
ACCESS_TOKEN_TTL="15m"
REFRESH_TOKEN_TTL="30d"
